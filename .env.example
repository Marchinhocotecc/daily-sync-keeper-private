# Copy this file to ".env.local" for development and fill in secrets. Do NOT commit ".env.local".
# For production, use ".env.production.local" locally on the build machine.

# === REQUIRED (Auth + core) ===
# IMPORTANT: This must be your Supabase project URL, not your Vite dev server.
# Example: https://abcd1234.supabase.co
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
VITE_WEATHER_API_KEY=YOUR_WEATHER_API_KEY

# === OPTIONAL ===
VITE_GOOGLE_REDIRECT_URI=http://localhost:8080/auth/callback
VITE_GOOGLE_CALENDAR_API_URL=https://www.googleapis.com/calendar/v3
VITE_ENABLE_REMOTE=1
VITE_DEFAULT_LANGUAGE=en
VITE_APP_NAME=Daily-sync-keeper
VITE_APP_DESCRIPTION=Your personal assistant for managing daily tasks, weather updates, and calendar events seamlessly.

# AI / Mistral
VITE_MISTRAL_API_URL=https://api.mistral.ai/v1
VITE_MISTRAL_API_KEY=YOUR_MISTRAL_API_KEY
VITE_MISTRAL_MODEL=mistral-large-latest
# Fallback alias (legacy). If set, it will be used only when VITE_MISTRAL_API_KEY is not provided.
VITE_AI_API_KEY=

# NOTE:
# - Do not point VITE_SUPABASE_URL to your dev server (e.g., :5173). Use your Supabase project URL.
# - Supabase OAuth redirect must match one of the Authorized Redirect URIs in Google Cloud Console.
# - For local dev add: http://localhost:8080/auth/callback (or your dev port).
# - After Google login Supabase will redirect back; onAuthStateChange listener updates global state.
# - Prefer VITE_MISTRAL_API_KEY over VITE_AI_API_KEY. The latter is kept only as a fallback.
# - Do NOT add GOOGLE_CLIENT_SECRET or sensitive server-only keys here.
